envs:
    - name: H2DATABASE_DRIVER_VERSION
      description: "The version of the `com.h2database:h2` Maven artifact. When set, this version is used instead of the default version bundled with the feature pack."
      required: false
      build-time: true
      alternate-system-property: "org.wildfly.datasources.h2database.driver.version"
